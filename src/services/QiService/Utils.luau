local ReplicatedStorage = game:GetService("ReplicatedStorage")

local BigNum = require(ReplicatedStorage.Shared.BigNum)
local Data = require(ReplicatedStorage.Shared.Data).client

local QiServiceUtils = {}
